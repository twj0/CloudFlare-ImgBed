# Cloudflare Pages Redirects Configuration
# This file handles routing for the macOS Finder-style image bed

# Let Pages Functions handle /api/* and /file/* automatically (no redirect needed)
# Keep SPA fallback for non-function routes
/* /index.html 200
